import React, { useState, useEffect } from "react";

const SearchWidget = () => {
    //initialize state
    const [term, setTerm ] = useState('')
    const [results, setResults] = useState([])

    return (
        <div>Here's a search component</div>
    )
}

export default SearchWidget